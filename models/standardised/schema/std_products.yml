version: 2

models:
    - name: std_products
      description: "Cleaned/standardised product information"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null