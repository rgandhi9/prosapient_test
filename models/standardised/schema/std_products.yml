version: 2

# See std_customers for how data tests can be used

models:
    - name: std_products
      description: "Cleaned/standardised product information"
      columns:
          - name: product_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null